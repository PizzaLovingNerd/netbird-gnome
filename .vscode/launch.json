{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open nested GNOME",
      "type": "node-terminal",
      "command": "dbus-run-session -- gnome-shell --nested --wayland",
      "request": "launch"
    }
  ]
}
